# HTTP port the signaling server listens on
PORT=8787
# Origin allowed to connect to the signaling server (the Vite dev server)
ALLOWED_ORIGIN=http://localhost:5173
# Port the Vite client dev server listens on
CLIENT_PORT=5173
